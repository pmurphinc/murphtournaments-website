import { useState } from "react";
import { Crown, LogIn, Shield, UserPlus, Users } from "lucide-react";
import { toast } from "sonner";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { trpc } from "@/lib/trpc";
import { useAuth } from "@/_core/hooks/useAuth";
import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger } from "@/components/ui/alert-dialog";

const initials = (name: string) => name.split(/\s+/).map(part => part[0]).join("").slice(0, 2).toUpperCase() || "MT";
const displayUser = (user: { discordDisplayName?: string | null; discordUsername?: string | null; name?: string | null }) => user.discordDisplayName || user.discordUsername || user.name || "Discord user";

export default function TeamManagement() {
  const { user } = useAuth();
  const isDiscordUser = user?.loginMethod === "discord";
  const utils = trpc.useUtils();
  const [teamName, setTeamName] = useState("");
  const [renameByTeam, setRenameByTeam] = useState<Record<number, string>>({});
  const [inviteByTeam, setInviteByTeam] = useState<Record<number, string>>({});
  const query = trpc.teamManagement.myTeams.useQuery(undefined, { enabled: !!isDiscordUser });
  const invalidate = () => utils.teamManagement.myTeams.invalidate();
  const ok = (message: string) => { toast.success(message); invalidate(); };
  const create = trpc.teamManagement.create.useMutation({ onSuccess: () => { setTeamName(""); ok("Team created."); }, onError: e => toast.error(e.message) });
  const rename = trpc.teamManagement.rename.useMutation({ onSuccess: () => ok("Team renamed."), onError: e => toast.error(e.message) });
  const invite = trpc.teamManagement.inviteByDiscordUsername.useMutation({ onSuccess: () => ok("Invite sent."), onError: e => toast.error(e.message) });
  const cancelInvite = trpc.teamManagement.cancelInvite.useMutation({ onSuccess: () => ok("Invite canceled."), onError: e => toast.error(e.message) });
  const removeMember = trpc.teamManagement.removeMember.useMutation({ onSuccess: () => ok("Member removed."), onError: e => toast.error(e.message) });
  const transferCaptain = trpc.teamManagement.transferCaptain.useMutation({ onSuccess: () => ok("Captain transferred."), onError: e => toast.error(e.message) });
  const disband = trpc.teamManagement.disband.useMutation({ onSuccess: () => ok("Team disbanded."), onError: e => toast.error(e.message) });
  const respond = trpc.teamManagement.respondToInvite.useMutation({ onSuccess: () => ok("Invite updated."), onError: e => toast.error(e.message) });
  const leave = trpc.teamManagement.leaveTeam.useMutation({ onSuccess: () => ok("You left the team."), onError: e => toast.error(e.message) });

  if (!isDiscordUser) return <section className="container py-16"><Card className="mx-auto max-w-xl border-yellow-400/30 bg-black/70 text-white"><CardHeader><CardTitle className="font-display text-3xl text-yellow-300">Team Management</CardTitle></CardHeader><CardContent className="space-y-4"><p className="text-white/70">Sign in with Discord to create teams, manage rosters, and accept invitations.</p><Button asChild><a href="/api/auth/discord/login"><LogIn className="mr-2 h-4 w-4" /> Continue with Discord</a></Button></CardContent></Card></section>;

  return <section className="container py-10 text-white"><div className="mb-8"><p className="font-mono text-sm uppercase tracking-[0.3em] text-yellow-400">Discord roster tools</p><h1 className="font-display text-4xl md:text-6xl">Team Management</h1></div>
    <div className="grid gap-6 lg:grid-cols-[360px_1fr]"><div className="space-y-6"><Card className="border-yellow-400/30 bg-black/70 text-white"><CardHeader><CardTitle className="flex items-center gap-2"><Users className="h-5 w-5 text-yellow-300" /> Create Team</CardTitle></CardHeader><CardContent className="space-y-3"><Input value={teamName} onChange={e => setTeamName(e.target.value)} placeholder="Team Name" maxLength={64} /><Button className="w-full" onClick={() => create.mutate({ name: teamName })} disabled={create.isPending || teamName.trim().length < 2}>Create Team</Button></CardContent></Card>
    <Card className="border-neon-cyan/30 bg-black/70 text-white"><CardHeader><CardTitle>Pending Invites</CardTitle></CardHeader><CardContent className="space-y-3">{query.data?.receivedInvites.length ? query.data.receivedInvites.map(inv => <div key={inv.id} className="rounded-lg border border-white/10 bg-white/5 p-3"><p className="font-bold">{inv.team.name}</p><p className="text-xs text-white/55">From {displayUser(inv.createdBy)}</p><div className="mt-3 flex gap-2"><Button size="sm" onClick={() => respond.mutate({ inviteId: inv.id, response: "accept" })}>Accept</Button><Button size="sm" variant="outline" onClick={() => respond.mutate({ inviteId: inv.id, response: "decline" })}>Decline</Button></div></div>) : <p className="text-sm text-white/60">No pending invitations.</p>}</CardContent></Card></div>
    <div className="space-y-6">{query.isLoading ? <p>Loading teams...</p> : query.data?.teams.length ? query.data.teams.map(team => { const isCaptain = team.role === "captain"; return <Card key={team.id} className="overflow-hidden border-yellow-400/25 bg-black/75 text-white"><CardHeader className="border-b border-white/10 bg-gradient-to-r from-yellow-400/10 to-transparent"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-4"><div className="flex h-14 w-14 items-center justify-center rounded-full border border-yellow-300 bg-yellow-400/15 font-display text-xl text-yellow-200">{initials(team.name)}</div><div><CardTitle>{team.name}</CardTitle><p className="text-xs uppercase tracking-widest text-white/50">/{team.slug}</p></div></div>{isCaptain ? <span className="flex items-center gap-1 text-sm text-yellow-300"><Crown className="h-4 w-4" /> Captain</span> : <AlertDialog><AlertDialogTrigger asChild><Button variant="outline">Leave Team</Button></AlertDialogTrigger><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>Leave {team.name}?</AlertDialogTitle><AlertDialogDescription>You will need a new invite to rejoin.</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel>Cancel</AlertDialogCancel><AlertDialogAction onClick={() => leave.mutate({ teamId: team.id })}>Leave Team</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog>}</div></CardHeader><CardContent className="space-y-5 p-5">{isCaptain ? <div className="grid gap-3 md:grid-cols-2"><div className="flex gap-2"><Input value={renameByTeam[team.id] ?? team.name} onChange={e => setRenameByTeam({ ...renameByTeam, [team.id]: e.target.value })} /><Button onClick={() => rename.mutate({ teamId: team.id, name: renameByTeam[team.id] ?? team.name })}>Rename</Button></div><div className="flex gap-2"><Input value={inviteByTeam[team.id] ?? ""} onChange={e => setInviteByTeam({ ...inviteByTeam, [team.id]: e.target.value })} placeholder="Discord username" /><Button onClick={() => invite.mutate({ teamId: team.id, discordUsername: inviteByTeam[team.id] ?? "" })}><UserPlus className="h-4 w-4" /></Button></div></div> : null}<div><h3 className="mb-2 font-mono text-sm uppercase text-yellow-300">Roster</h3><div className="grid gap-2">{team.roster.map((row: any) => <div key={row.userId} className="flex flex-wrap items-center justify-between gap-3 rounded-lg border border-white/10 bg-white/5 p-3"><div className="flex min-w-0 items-center gap-3">{row.user.discordAvatarUrl ? <img src={row.user.discordAvatarUrl} alt="" className="h-10 w-10 rounded-full border border-neon-cyan/40" /> : <div className="h-10 w-10 rounded-full bg-yellow-400/20" />}<div className="min-w-0"><p className="truncate font-semibold">{displayUser(row.user)}</p><p className="text-xs text-white/50">{row.user.discordUsername ? `@${row.user.discordUsername}` : "Discord"}</p></div>{row.role === "captain" ? <Shield className="h-4 w-4 text-yellow-300" /> : null}</div>{isCaptain && row.userId !== user?.id ? <div className="flex gap-2"><Button size="sm" variant="outline" onClick={() => transferCaptain.mutate({ teamId: team.id, newCaptainUserId: row.userId })}>Make Captain</Button><AlertDialog><AlertDialogTrigger asChild><Button size="sm" variant="destructive">Remove</Button></AlertDialogTrigger><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>Remove {displayUser(row.user)}?</AlertDialogTitle><AlertDialogDescription>This removes the player from the roster.</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel>Cancel</AlertDialogCancel><AlertDialogAction onClick={() => removeMember.mutate({ teamId: team.id, memberUserId: row.userId })}>Remove Member</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog></div> : null}</div>)}</div></div><div><h3 className="mb-2 font-mono text-sm uppercase text-yellow-300">Pending Invitations</h3>{team.pendingInvites.length ? team.pendingInvites.map((inv: any) => <div key={inv.id} className="mb-2 flex items-center justify-between rounded-lg border border-white/10 bg-white/5 p-3"><span>{displayUser(inv.invitedUser)}</span>{isCaptain ? <Button size="sm" variant="outline" onClick={() => cancelInvite.mutate({ inviteId: inv.id })}>Cancel</Button> : null}</div>) : <p className="text-sm text-white/55">No pending invitations.</p>}</div>{isCaptain ? <AlertDialog><AlertDialogTrigger asChild><Button variant="destructive">Disband Team</Button></AlertDialogTrigger><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>Disband {team.name}?</AlertDialogTitle><AlertDialogDescription>This permanently deletes the managed team and roster.</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel>Cancel</AlertDialogCancel><AlertDialogAction onClick={() => disband.mutate({ teamId: team.id, confirmation: "DISBAND" })}>Disband Team</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog> : null}</CardContent></Card>; }) : <Card className="border-yellow-400/30 bg-black/70 text-white"><CardContent className="p-8 text-center"><p className="mb-4 text-white/70">You are not on any managed teams yet.</p><Button onClick={() => document.querySelector<HTMLInputElement>('input[placeholder="Team Name"]')?.focus()}>Create Team</Button></CardContent></Card>}</div></div></section>;
}
